// Site Worker: static assets + /api/* (contact form, and checkout for shops).
// Behaviour lives in the framework so fixes arrive with a framework version bump.
export { default } from "@zagware/site-framework/worker";
